
var base = 'http://192.168.0.107:9096';

window.g = {
  baseUrls: base,

  // 如果还有其他的公共路径，可以参考下面的格式，添加
  // baseUrl: base + '/iplatform-workflow',
  // baseUrl_fileLoad: base,
  // feaReplyController: base + '/epms-apply'
};
